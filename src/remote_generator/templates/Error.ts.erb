// imports needed for context types...

interface ErrorT {
  key: string
  symbol: string
  category: "data" | "runtime"
  context: any
  path: []
  runtime_path: []
}

/*
interface <%= error_name %><
  keyT extends string,
  pathT extends string[] = [],
  runtime_pathT extends string[] = [],
> extends <%= runtime_error? ? "Runtime" : "Data" %>Error {
  key: keyT
  symbol: "<%= symbol %>"
  path: pathT
  runtime_path: runtime_pathT
  context: <%= context_ts_type %>
}
