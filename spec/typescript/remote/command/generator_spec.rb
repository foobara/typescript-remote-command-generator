RSpec.describe Typescript::Remote::Command::Generator do
  it "has a version number" do
    expect(Typescript::Remote::Command::Generator::VERSION).to_not be_nil
  end

  it "does something useful" do
    expect(false).to be(true)
  end
end
