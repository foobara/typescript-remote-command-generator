# frozen_string_literal: true

RSpec.describe Typescript::Remote::Command::Generator do
  it "has a version number" do
    expect(Typescript::Remote::Command::Generator::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
